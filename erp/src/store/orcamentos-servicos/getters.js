export const module = (state) => {
  return state.module
}

export const list = (state) => {
  return state.list
}

export const item = (state) => {
  return state.item
}

export const loading = (state) => {
  return state.loading
}

export const filter = (state) => {
  return state.filter
}

export const where = (state) => {
  return state.where
}

export const whereHas = (state) => {
  return state.whereHas
}

export const pagination = (state) => {
  return state.pagination
}

export const currentId = (state) => {
  return state.currentId
}

export const listAbertos = (state) => {
  return state.listAbertos
}

export const listDivergencias = (state) => {
  return state.listDivergencias
}
